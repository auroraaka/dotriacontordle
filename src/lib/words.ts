/**
 * Word validation module
 * Uses the dynamic word service with Datamuse API for validation
 */

export { isValidWord, isValidWordSync, quickCheckWord, initializeWordService } from './wordService';
